(jbuild_version 1)

;; This declare the hello_world executable implemented by hello_world.ml
(executables
 ((names (test))
  (libraries (core_kernel alcotest))  
  ))

(alias
 ((name DEFAULT)
(deps (test.exe))))
